import { describe, expect, test } from "@jest/globals";

describe("Simple Update Strategy tests", () => {
  test("Push new prices works fine", async () => {
    // @TODO: Add tests
    expect(true).toBe(true);
  });
});
